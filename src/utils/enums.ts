export enum btnType {
    valid = 'valid',
    error = 'error',
    cancel = 'cancel',
    primary = 'primary'
}
export enum inputType {
    text = 'text',
    textarea = 'textarea',
    select = 'select',
    date = 'date'
}
export enum modalClasses {
    small = 'small',
    medium = 'medium',
    big = 'big',
    invisible = 'invisible',
}
export enum methodTypes {
    post = 'POST',
    get = 'GET'
}
